{
  "name": "callback-flowcontrol-middewares",
  "version": "0.0.1",
  "description": "flow control middewares for callbacks",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "files": [
    "src/**"
  ],
  "scripts": {
    "start": "tsc && concurrently \"nodemon ./src/index.js\" \"npm run watch-ts\"",
    "watch-ts": "tsc --watch",
    "build": "tsc -p ./tsconfig.prod.json",
    "build-watch": "tsc --watch -p ./tsconfig.prod.json",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^23.3.7",
    "@types/node": "^10.12.2",
    "concurrently": "^4.0.1",
    "jest": "^23.6.0",
    "nodemon": "^1.18.5",
    "ts-jest": "^23.10.4",
    "typescript": "^3.1.3"
  }
}
